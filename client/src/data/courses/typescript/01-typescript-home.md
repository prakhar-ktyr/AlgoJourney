---
title: TypeScript Tutorial
---

# TypeScript Tutorial

TypeScript is a syntactic superset of JavaScript which adds **static typing**.

This means that TypeScript adds syntax on top of JavaScript, allowing developers to add **types**.

TypeScript being a "Syntactic Superset" means that it shares the same base syntax as JavaScript, but adds something to it.

---

## Why Should I Use TypeScript?

JavaScript is a loosely typed language. It can be difficult to understand what types of data are being passed around in JavaScript.

In JavaScript, function parameters and variables don't have any information! So developers need to look at documentation, or guess based on the implementation.

TypeScript allows specifying the types of data being passed around within the code, and has the ability to report errors when the types don't match.

For example, TypeScript will report an error when passing a string into a function that expects a number. JavaScript will not.

> [!NOTE]
> TypeScript code is transformed into JavaScript code via the TypeScript compiler or babel. This process is called **transpilation**.

---

## How to use this Tutorial

This tutorial is designed for both beginners and experienced professionals.

It is divided into steps:

1. **Basics**: Learn the syntax and primitive types.
2. **Complex Types**: Work with Arrays, Tuples, and Objects.
3. **Functions & Interfaces**: Structure your applications.
4. **Classes**: Master Object-Oriented TypeScript.
5. **Generics**: Build highly reusable components.
6. **Advanced & Utility Types**: Dive deep into type manipulation.
7. **Config & Real-World Usage**: Learn to set up and deploy TS in React or the DOM.

Let's get started with learning TypeScript!
